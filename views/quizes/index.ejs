<table>
<% var i; for (i=0; i<quizes.length; i++){ %>
  <tr>
    <td class="icon-<%= quizes[i].tematica %>"></td>
    <td><a href="quizes/<%= quizes[i].id%>"><%= quizes[i].pregunta %></a></td>
    <%if (session.user) {%>
    <td><a href="quizes/<%= quizes[i].id%>/edit"><button>editar</button></a></td>
    <td>
      <form method="post" action="quizes/<%= quizes[i].id %>?_method=delete">
        <button type="submit" onClick="return confirm('Borrar: <%= quizes[i].pregunta%>');">
          borrar
        </button>
      </form>
    </td>
    <%}%>
</tr>
<% } %>
</table>
<form method="get" action="/quizes">
    <p>Búsqueda</p>
    <input type="text" name = "search" placeholder ="Introduce el texto de la pregunta a buscar"/>
    <input type="submit" value="Enviar"/>
</form>
<%if (session.user) {%>
<a href="/quizes/new"><button type="button"> Crear pregunta </button></a>
<%}%>
<div id="leyenda">
  <ul>
    <li class='icon-ciencia'>Ciencia</li>
    <li class='icon-humanidades'>Humanidades</li>
    <li class='icon-ocio'>Ocio</li>
    <li class='icon-otro'>Otro</li>
    <li class='icon-tecnologia'>Tecnología</li>
  </ul>
</div>
